name: offline_barcode_cashier_camera_fix
description: Offline barcode cashier and inventory app with robust live scanning.
publish_to: "none"
version: 1.1.0+18
environment:
  sdk: ">=3.7.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  mobile_scanner: ^7.4.0
  image_picker: ^1.1.2
  share_plus: ^10.1.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
